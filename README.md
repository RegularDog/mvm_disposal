put the map file (.bsp) in your tf/download/maps folder;

put the mission files (.pop) in your tf/scripts/population folder (if you don't have it then create a new one);

put the classicons (.vmt/.vtf) in your tf/download/materials/hud folder.
